# cosmos-query.yaml — Drasi ContinuousQuery using Cypher over the Cosmos DB source
apiVersion: v1
kind: ContinuousQuery
metadata:
  name: cosmos-query
spec:
  mode: query
  sources:
    subscriptions:
      - id: cosmos-source
  query: >
    MATCH (n)
    RETURN n.id AS id, n.label AS label
