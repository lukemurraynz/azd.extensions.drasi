name: aks-production
version: 1.0.0
description: AKS incident diagnostics and proactive health monitoring capability pack.
depends_on:
  - base-core
resources:
  agents:
    - ./agents/aks-diagnostics.yaml
  response_plans:
    - ./response-plans/aks-critical.yaml
  scheduled_tasks:
    - ./scheduled-tasks/aks-daily-health.yaml
