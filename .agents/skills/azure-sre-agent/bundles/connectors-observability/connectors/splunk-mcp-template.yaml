api_version: azuresre.ai/v1
kind: DataConnector
spec:
  name: splunk-mcp
  type: Mcp
  endpoint: https://<SPLUNK_HOST>:8089/mcp/v1
  auth:
    type: Header
    headerName: Authorization
    headerValue: Bearer <SPLUNK_AUTH_TOKEN>

# Ensure Splunk token has search and knowledge object permissions.
