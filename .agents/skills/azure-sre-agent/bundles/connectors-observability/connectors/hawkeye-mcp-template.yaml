api_version: azuresre.ai/v1
kind: DataConnector
spec:
  name: hawkeye-mcp
  type: Mcp
  endpoint: https://<HAWKEYE_ENDPOINT>/mcp/v1
  auth:
    type: Header
    headerName: Authorization
    headerValue: Bearer <HAWKEYE_API_TOKEN>

# Configure Hawkeye token with observability read permissions.
