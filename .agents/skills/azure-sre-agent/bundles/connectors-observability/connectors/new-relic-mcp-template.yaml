api_version: azuresre.ai/v1
kind: DataConnector
spec:
  name: new-relic-mcp
  type: Mcp
  endpoint: https://api.newrelic.com/mcp/v1
  auth:
    type: Header
    headerName: Api-Key
    headerValue: <NEW_RELIC_USER_API_KEY>

# Ensure New Relic user API key has query and dashboard permissions.
