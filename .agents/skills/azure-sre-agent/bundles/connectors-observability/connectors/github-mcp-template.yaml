api_version: azuresre.ai/v1
kind: DataConnector
spec:
  name: github-mcp
  type: Mcp
  endpoint: https://api.githubcopilot.com/mcp/
  auth:
    type: BearerToken
    token: <GITHUB_PAT_OR_TOKEN>

# Use service-owned token where possible and rotate on schedule.
