# drasi.yaml - Drasi project manifest
apiVersion: v1
kind: DrasiProject
includes:
  - kind: sources
    pattern: "sources/*.yaml"
  - kind: queries
    pattern: "queries/*.yaml"
  - kind: reactions
    pattern: "reactions/*.yaml"
environments:
  # staging:
  #   components:
  #     - kind: source
  #       id: my-source
  #       properties:
  #         connectionString: "staging-connection-string"
featureFlags: {}
