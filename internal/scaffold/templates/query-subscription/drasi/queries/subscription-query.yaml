# subscription-query.yaml - ContinuousQuery for the subscription pattern
apiVersion: v1
kind: ContinuousQuery
name: subscription-query
spec:
  mode: query
  sources:
    subscriptions:
      - id: example-source
  query: |
    MATCH (n)
    RETURN n.name AS name
