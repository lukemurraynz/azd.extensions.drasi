# example-query.yaml - Drasi ContinuousQuery definition
apiVersion: v1
kind: ContinuousQuery
name: example-query
spec:
  mode: query
  sources:
    subscriptions:
      - id: example-source
  query: |
    MATCH (n)
    RETURN n.name AS name
