# cosmos-query.yaml - ContinuousQuery over Cosmos DB source using Cypher
apiVersion: v1
kind: ContinuousQuery
name: cosmos-query
spec:
  mode: query
  sources:
    subscriptions:
      - id: cosmos-source
  query: |
    MATCH (n)
    RETURN n.id AS id, n.label AS label
