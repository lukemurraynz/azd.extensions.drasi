apiVersion: v1
kind: ContinuousQuery
name: order-changes
spec:
  mode: query
  sources:
    subscriptions:
      - id: postgres-source
  query: |
    MATCH (o:Order) RETURN o.id
  reactions:
    - pubsub-orders
